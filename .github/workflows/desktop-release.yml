name: Build Desktop App

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'  # updated to Node 24 to avoid deprecated Node 20 warning

      - name: Install dependencies
        working-directory: desktop
        run: npm install

      - name: Build Mac app (.dmg)
        working-directory: desktop
        env:
          LUCY_URL: https://lucy.replit.app
        run: npm run build:mac

      - name: Upload .dmg artifact
        uses: actions/upload-artifact@v4
        with:
          name: lucy-mac-dmg
          path: desktop/dist/*.dmg

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: desktop/dist/*.dmg
          generate_release_notes: true
<<<<<<< HEAD
          token: ${{ secrets.GH_TOKEN }}  # <<-- USE YOUR PERSONAL TOKEN HERE
=======
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
>>>>>>> f9dac4a (Update GitHub Actions workflow to use correct token for releases)

  build-win:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        working-directory: desktop
        run: npm install

      - name: Build Windows app (.exe)
        working-directory: desktop
        env:
          LUCY_URL: https://lucy.replit.app
        run: npm run build:win

      - name: Upload .exe artifact
        uses: actions/upload-artifact@v4
        with:
          name: lucy-win-installer
          path: desktop/dist/*.exe

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: desktop/dist/*.exe
          generate_release_notes: true
<<<<<<< HEAD
          token: ${{ secrets.GH_TOKEN }}  # <<-- USE YOUR PERSONAL TOKEN HERE
=======
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
>>>>>>> f9dac4a (Update GitHub Actions workflow to use correct token for releases)
